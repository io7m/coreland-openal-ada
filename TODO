Complete unit tests.

Decide how to handle extensions (child packages likely).

  OpenAL.Extensions.*

OpenAL.Context

  Allow attribute list passing to Open_Device.

  ALCenum alcGetEnumValue(const ALCdevice *deviceHandle, const ALCchar *enumName);

    ALC_ATTRIBUTES_SIZE

  void alcGetIntegerv(ALCdevice * deviceHandle, ALCenum token, ALCsizei size, ALCint *dest);

    ALC_ALL_ATTRIBUTES
